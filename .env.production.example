# Production env (set on Vercel project, not committed at runtime).
# api.005917.xyz is the public HTTPS hostname for the Oracle Cloud backend
# (Baota Nginx reverse proxy + Let's Encrypt → 127.0.0.1:8000 FastAPI).
NEXT_PUBLIC_API_MODE=real
NEXT_PUBLIC_API_URL=https://api.005917.xyz
